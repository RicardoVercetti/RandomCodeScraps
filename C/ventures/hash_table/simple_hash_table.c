#include <stdio.h>

// hashtable
// array of an object T
// T → { key: String, value: U }

// hashing algorithm

int main() {
    printf("simple hash table...\n");

    // allocate memory and create a hashtable
    
    // push to it with key

    // find elements with key

    // .len()

    // destroy the hashmap and freeup all the memory
    printf("eof: %x\n", EOF);

    return 0;
}

