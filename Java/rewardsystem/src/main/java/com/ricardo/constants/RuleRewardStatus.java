package com.ricardo.constants;

/**
 * There are several rules and rewards that could be added to a Loyalty Program.
 * These rules or rewards can be set not to be checked with their {@code RuleRewardStatus}
 * being {@code ACTIVE} or {@code INACTIVE}
 */
public enum RuleRewardStatus {
    ACTIVE,
    INACTIVE
}
